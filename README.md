Download the repository, open it with your preferred console, inside the human-movies folder perform "npm install" to install the packages. After that, perform "npm start" to initialize the application.